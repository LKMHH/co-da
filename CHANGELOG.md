# CODA 更新日志（CHANGELOG）

> 规则：每次发版在顶部加一条。格式见 v0.1.0 条目。版本号与 `app/build.gradle.kts` 的 `versionName` 保持一致。

## [0.2.0] - 2026-08-15

首个发布候选：M7 备份恢复 + M8 收口 + 四份规格稿与代码一致性修复 + 视觉规范落地。

### 新增
- **备份与恢复（M7）**：导出 `.coda-backup`、导入前数量预览、安全备份（保留 3 份）、journal 中断回滚
- **普通工作增强**：补充信息可填设备；编辑可改工作日期（yyyy-MM-dd 校验）
- **派生工作记录**：内容含处理摘要（根因/措施），详情展示处理起止时间
- **交接**：「继续处理故障」真实创建同一故障的新处理记录
- **通知**：Android 13+ 首次启动引导一次系统授权，被拒后设置页可重新申请
- **作废统一入口**：首页「包含已作废」扩展到故障/交接/工作，作废项标红显示
- **视觉规范**：11 个冻结语义色、七级字号体系、8dp 圆角上限、表单页固定底部操作区、结束处理底部表单 + 单选组
- **启动页**：全屏启动图约 1.6 秒后淡出，期间首页正常加载
- **应用图标**：接入自定义图标（传统原图）
- **首次使用引导**：四步引导（记录工作/记录故障/写交接/备份恢复），第 4 步可直达备份；仅首启显示一次，可跳过
- **全局按钮 52dp**：主/次按钮统一高度，对齐视觉稿 §1.3

### 修复
- M8 验收清单失效引用（产品稿 §12.13 → §12 第 13 条；`.dianqi-backup` → `.coda-backup`）
- 恢复结果文案统一为「无法确认」
- schema 事实来源收口（回调 DDL 清单化 + SchemaParityTest 守护，迁移必重放）
- 技术稿包结构过期回改、ARCHITECTURE.md 同步、视觉稿 §19.2 导航口径修正
- 启动页裁剪底部水印

### 技术说明
- 数据库版本：1（本轮无表结构变更、无迁移需求）
- 单元测试 155 个全绿；纯本地运行，不声明 INTERNET 权限

## [0.1.0] - M1–M6 内部里程碑（未发布）

首个可安装的内部版本，覆盖设计稿 v1.1 前六个里程碑：

### 新增
- **核心规则层（M1）**：故障状态机、文本规范化、业务日期与排班纯函数
- **本地数据层（M2）**：Room 数据库（10 张表）、设备、出勤、故障草稿自动保存与进程恢复
- **故障闭环（M3）**：快速记录三字段 60 秒保存、处理过程补充、完成事务（自动生成工作记录、未恢复自动转交接）
- **今日工作台（M4）**：首页聚合、自然日/本次出勤视图、统一工作列表、进行中故障卡片
- **排班与通知（M5）**：月度排班、转班、闹钟调度、通知权限降级后应用内提醒
- **搜索（M6）**：连续子串匹配、同义词展开、设备别名命中、作废记录默认隐藏

### 技术说明
- 数据库版本：1（迁移体系见 `VERSIONING.md`）
- 纯本地运行，不声明 INTERNET 权限

<!-- 新版本从这里往上加：

## [0.1.1] - YYYY-MM-DD
### 修复
- ...
### 新增
- ...

-->
