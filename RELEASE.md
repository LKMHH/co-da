# CODA 发版检查清单（RELEASE）

> 每次发版前逐项打勾。任何一项失败 → 停下来修，不带着问题发版。
> 配套文档：`VERSIONING.md`（版本号规则）、`CHANGELOG.md`（更新日志）。

## 0. 发版前决策

- [ ] 确认这次改了什么（对照 CHANGELOG 新增条目）
- [ ] 版本号已按 `VERSIONING.md` 决策表更新（versionName / versionCode / 数据库 version）
- [ ] 数据库动了 → 已写 Migration，并确认**没有** `fallbackToDestructiveMigration`

## 1. 数据安全

- [ ] keystore 文件存在且已备份（项目外至少一份，如 U 盘/网盘）
- [ ] 签名与上一个发布版本**完全一致**（同一把 keystore）
- [ ] 升级路径测试：用装着**旧版本真实数据**的手机，覆盖安装新 APK
      - 数据完整（故障记录/工作记录/交接/资料全在）
      - 老版本 → 新版本 → 再下一个新版本，每条路径都测
- [ ] 备份功能自测：导出 `.coda-backup` → 删除数据 → 导入恢复成功

## 2. 功能回归（M8 核心项）

- [ ] 飞行模式下：快速记录故障 ≤ 60 秒（三字段）
- [ ] 搜索：预置词（如"信号弱"）命中预期记录，<1 秒返回
- [ ] 完成一条故障 → 当天工作记录自动出现，可打开原始记录
- [ ] 未恢复故障 → 一键转交接，字段自动带入
- [ ] 备份导出/导入：含 1 张照片 + 1 份 PDF，导入后均可打开
- [ ] 进程被杀（从最近任务划掉）→ 重开记录仍在；编辑中强杀 → 草稿可恢复

## 3. 打包与发布

- [ ] `CHANGELOG.md` 已更新（这版改了啥，三行以上）
- [ ] 运行 `scripts\release.bat` 打包成功，APK 路径与大小确认
- [ ] 安装到至少一台真机验证（不是只装模拟器）
- [ ] 确认 APK 安装后版本号显示正确（设置页或系统应用信息）

## 4. 发布动作

- [ ] 打 git tag：`git tag v<versionName>` 并推送
- [ ] 微信群发布：APK 文件 + 三行更新说明（改了啥 / 怎么装 / 注意啥）
- [ ] 提醒同事：升级前建议导出一次备份（卸载即丢失）

## 5. 发布后

- [ ] 收集反馈，进入下一轮迭代（P0/P1/P2 分级）
- [ ] 观察两天：有 P0 问题 → 立即出修复小版本，不攒
