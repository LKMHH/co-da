# CODA 版本管理规范（VERSIONING）

> 本文件规定每次改版必须先读的规则。改版前 30 秒过一遍，避免发出版本混乱。

## 1. 版本号三兄弟

| 编号 | 位置 | 给谁看 | 规则 |
|------|------|--------|------|
| `versionName` | `app/build.gradle.kts` | 用户（设置页显示） | 语义化：`主.功能.修复` |
| `versionCode` | `app/build.gradle.kts` | Android 系统（判断新旧） | **只增不减**，每次发版 +1 |
| 数据库 `version` | `CodaDatabase.kt` 的 `@Database(version=...)` | Room（判断迁移） | **改了表结构才 +1**，且必须写对应 Migration |

## 2. 改版决策表

| 这次改了什么 | versionName 变化 | versionCode | 数据库 version |
|--------------|------------------|-------------|----------------|
| 修 bug（闪退、搜索慢、显示错） | `0.1.0 → 0.1.1` | +1 | 不动 |
| 加小功能（不改数据结构） | `0.1.1 → 0.2.0` | +1 | 不动 |
| 加功能且改了表结构 | `0.2.0 → 0.3.0` | +1 | +1，写 Migration |
| 大改版（如 v2.0 引入协作/网络） | `0.3.0 → 1.0.0` | +1 | 大概率 +1 |

**铁律**：
- `versionCode` 永不回退、永不复用。升上去了就保持。
- 数据库 `version` 动了，**必须**写 `MIGRATION_x_y` 并跑迁移测试，见 `RELEASE.md`。
- 严禁 `fallbackToDestructiveMigration()` —— 它会在升级时清空用户数据。

## 3. 一次迭代的标准流水线

```
① 需求进池（微信群反馈 → 整理成清单，标 P0/P1/P2）
② 定版本号（按上表决策）
③ 开发（改代码 + 补单元测试）
④ 数据库动了？→ 写 Migration + 跑迁移测试
⑤ 真机回归（M8 验收矩阵核心项：快速记录 / 搜索 / 自动工作记录 / 备份导入导出）
⑥ 更新 CHANGELOG.md（三行：这版改了啥）
⑦ 打包 release APK（必须同一把签名，见 scripts/release.bat）
⑧ 发版：微信群发 APK + 版本说明
```

## 4. 数据库迁移三件套

1. **手写 Migration**：`MIGRATION_1_2`、`MIGRATION_2_3`…… 每段只负责相邻两个版本。
2. **升级路径测试矩阵**：发版前用装着旧数据的手机覆盖安装。**每条历史路径都要测**（用户可能跳过多个版本），不是只测最新路径。
3. **schema 导出已开启**（`exportSchema = true`，`app/schemas/` 下有 `1.json`），迁移测试的地基已就位。

## 5. Git 协作规范

- 一个迭代一个 commit，message 写清楚：`feat: ...` / `fix: ...` / `docs: ...`
- 每次发版打 tag：`git tag v0.1.1`，随时能找回任何历史版本
- AI（Codex 等）改完代码必须立即 commit，不攒改动
- keystore 严禁提交进仓库（.gitignore 已排除 `*.jks`）

## 6. 反馈闭环与发版节奏

- 反馈分级：P0（数据丢失/崩溃）当天修当天发；P1（功能不对）本周修；P2（体验优化）攒着每月发。
- 发版节奏：紧急 bug 单独小版本；新功能攒够一批再发。不天天发（用户烦），不憋太久（反馈凉）。
